name: Build and Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  IMAGE_REGISTRY: ghcr.io
  IMAGE_REPO: ghcr.io/0xpuncker/val-bittensor
  PYTHON_VERSION: "3.11"

jobs:
  sensors:
    name: Run Sensors
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install mypy ruff pytest pip-audit bandit[toml] detect-secrets
          pip install -r requirements.txt
          # Pre-install commonly used packages for faster subsequent runs
          pip install --upgrade bittensor kubernetes

      - name: Typecheck (mypy)
        run: mypy --config-file mypy.ini || true

      - name: Lint (ruff)
        run: ruff check .

      - name: Test (pytest)
        run: pytest --tb=short -q

      - name: Audit (pip-audit)
        run: pip-audit --desc

      - name: Security (bandit)
        run: bandit -r . -c pyproject.toml -ll

      - name: Secret scan
        run: detect-secrets scan --baseline .secrets.baseline || true

  build:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: sensors
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_REPO }}
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Extract image tag
        id: tag
        run: |
          TAG="${{ steps.meta.outputs.tags }}"
          # Get first tag (sha tag)
          IMAGE_TAG=$(echo "$TAG" | tr ' ' '\n' | head -n1)
          echo "tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT

      - name: Upload image tag artifact
        uses: actions/upload-artifact@v4
        with:
          name: image-tag
          path: ${{ steps.tag.outputs.tag }}

  update_k8s_manifests:
    name: Update K8s Manifests
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout val-bittensor
        uses: actions/checkout@v4

      - name: Download image tag
        uses: actions/download-artifact@v4
        with:
          name: image-tag
          path: ./artifact

      - name: Set IMAGE_TAG env
        run: |
          IMAGE_TAG=$(cat artifact/*)
          echo "IMAGE_TAG=${IMAGE_TAG}" >> $GITHUB_ENV
          # Extract short SHA for tag
          SHORT_SHA="${GITHUB_SHA:0:7}"
          echo "SHORT_SHA=${SHORT_SHA}" >> $GITHUB_ENV

      - name: Update K8s manifests
        run: |
          echo "Updating K8s manifests to use image tag: ${SHORT_SHA}"
          # Update bittensor-testnet.yaml
          sed -i "s|image: ghcr.io/0xpuncker/val-bittensor:.*|image: ghcr.io/0xpuncker/val-bittensor:${SHORT_SHA}|g" k8s/bittensor-testnet.yaml
          # Show the changes
          git diff k8s/bittensor-testnet.yaml

      - name: Commit and push K8s manifest update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add k8s/bittensor-testnet.yaml
          git diff --staged --quiet || git commit -m "chore(k8s): update image tag to ${SHORT_SHA} [ci skip]"
          git push
